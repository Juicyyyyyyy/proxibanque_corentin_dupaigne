Veuillez consulter le lien suivant pour accéder à la liste des endpoint de manière interactive :

http://localhost:8080/swagger-ui/index.html

---

La base de donnee est fake fill automatiquement a chaque fois que le serveur est run, grace a la classe DataSeeder.
Il est donc possible de tester les endpoint de get sur le swagger.